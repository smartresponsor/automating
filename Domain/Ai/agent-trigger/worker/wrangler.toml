# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp

name = "repo-agent-trigger"
main = "src/index.js"
compatibility_date = "2025-01-01"

[vars]
GH_OWNER = "marketing-america-corp"
GH_REPO = "repo"
GH_WORKFLOW = "sr-agent-dispatch.yml"
GH_REF = "master"

SR_TIME_SKEW_SEC = "300"
SR_ALLOWED_TASK = "scan,health,doctor,validate,plan,codex,pr"

# Set to "1" only for local dev; do NOT enable in prod.
SR_DEV_MODE = "0"
SR_DEBUG = "0"
